# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) への指針を提供する。

## プロジェクト概要

福岡市内の鉄道通勤者向けに、定期券の一番お得な買い方を判定する PWA。単独定期／連絡定期（1枚化）／分割定期などの候補を総額の安い順に提示し、月ごとの出勤日数から損益分岐、途中下車時の精算比較、払戻し概算まで扱う。ビルドツール無しの素の HTML/CSS/JS。

`build-app` という個人ワークスペース内の1プロジェクトとして管理されているが、それ自体は独立した git リポジトリ。ワークスペース横断の共通事項は親フォルダの `CLAUDE.md` を参照。

## コマンド

- **ローカル起動**: `ローカルサーバー起動.cmd`（または `server.ps1`）を実行 → http://localhost:8901/
  - `file://` で直接開くとインストール・オフライン動作ができないため、確認は必ずローカルサーバー経由で行う。
- **ビルド／lint／テスト**: 該当コマンド無し（ビルドツール未導入）。

## ディレクトリ構造

```
index.html
css/styles.css
js/data.js     路線・駅・連絡定期ルール・主要区間の定期代（初期めやす）・祝日・路線図の座標
js/app.js      経路探索・構成候補・出勤日数エンジン・戦略比較・途中下車・払戻し・色分けドロップダウン・UI
js/lib/        jsPDF, html2canvas（同梱・オフラインPDF用）
img/hero.webp  ヘッダーの福岡コラージュ（Nano Banana Pro 生成の実写風モンタージュ）
manifest.webmanifest / sw.js / icons/
server.ps1 + ローカルサーバー起動.cmd   ローカル確認用
docs/参考リンク.txt   参照した各社公式ページ
```

- 数値がおかしいときは `js/data.js`、挙動がおかしいときは `js/app.js` を疑う。
- 状態は `localStorage` にのみ保存（バックエンド・DB無し）。
- PDF出力は `js/lib/` 同梱の jsPDF + html2canvas でクライアント側完結。パッケージマネージャからは入れない。

## 注意事項

- `js/data.js` の `FARE_TABLE` は駅探／NAVITIME／各社公式を参照した「初期めやす」で、`src` に出典を記録している。出典を確認せずに数値だけを書き換えないこと。表に無い区間は `DISCOUNT_MODEL` で1ヶ月定期額から3/6ヶ月を概算する。
- 3社（西鉄〜地下鉄〜JR/貝塚線）通しの連絡定期は発売されないため分割になる、といった業務ルールを壊さないよう、計算ロジックの変更後は `README.md` 記載の想定ケースで確認する。
- 新しい画像（ヘッダー写真・アイコンなど）が必要な場合は、手描きやストック画像ではなく `generate-illustration` スキル（PC全体・`~/.claude/skills/generate-illustration/` に導入済み）で生成する。
